{
  "name": "todo-backend",
  "version": "1.0.0",
  "description": "The backend for todo",
  "main": "index.js",
  "scripts": {
    "start": "node src/index.js",
    "deploy": "rm -rf build && cd ../to-do/ && npm run build && cp -r build ../todo-backend/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "George",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "pg": "^8.5.1",
    "pg-hstore": "^2.3.3",
    "sequelize": "^6.5.0"
  }
}
